import type React from "react"
import type { Metadata } from "next"
import { Poppins } from "next/font/google"
import { Analytics } from "@vercel/analytics/next"
import "./globals.css"
import FloatingContactBar from "@/components/floating-contact-bar"

const poppins = Poppins({
  subsets: ["latin"],
  weight: ["400", "500", "600", "700"],
})

export const metadata: Metadata = {
  title: "Shree Ganesh Pest Control - Safe & Reliable Pest Solutions",
  description:
    "Professional pest control services for homes and businesses. Safe, eco-friendly, and certified solutions.",
  generator: "v0.app",
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`${poppins.className} font-sans antialiased`}>
        {children}
        <FloatingContactBar />
        <Analytics />
      </body>
    </html>
  )
}
